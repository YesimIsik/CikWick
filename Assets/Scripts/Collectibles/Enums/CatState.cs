using UnityEngine;

public enum CatState //enum (açýlýmý: enumeration), sabit bir grup deðeri temsil eden özel bir veri türüdür.
{ 

    Idle,//Kedinin durduðunu ifade eder.
    Walking,//Kedinin yürüdüðünü ifade eder.
    Running,//Kedinin koþtuðunu ifade eder.
   Attacking//Kedinin saldýrdýðýný ifade eder.
}
